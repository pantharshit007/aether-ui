import ComponentPreview from "@/components/web/component-preview";
import ScrollIndicatorDemo1 from "./scroll-indicator-demo-1";

import PrevNextBtn from "@/components/web/prev-next-btn";
import { metaConfig } from "@/config/site";

export const metadata = metaConfig({
  title: "Button | Aether UI",
  description: "A customizable button component with default | destructive variants.",
});

# Button

A simple customizable button component with **default** and **destructive** variants.

## Examples

<ComponentPreview
  component={<ScrollIndicatorDemo1 />}
  filePath="src/app/docs/scroll-indicator/scroll-indicator-demo-1.tsx"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<InstallationCli value="button" />

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>If you haven't completed the installation refer to guide [here](/docs/installation).</Step>

<Step>Copy and paste the following code into your project.</Step>

<CodeBlock filePath="src/components/content/scroll-indicator.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Component Props

You can customize the content of the `button` by passing the following props to the component.

<PrevNextBtn current="scroll-indicator" />
